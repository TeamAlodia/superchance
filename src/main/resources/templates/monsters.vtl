<h3>All Monsters</h3>
#foreach($monster in $monsters)
  <a href="">$monster.getName()</a>
#end

<h4>Dev Tools</h4>
<form class="" action="/monsters" method="post">
  <select class="" name="player_id">
    #foreach($player in $players)
      #thing_to_do
    #end
  </select>
</form>
